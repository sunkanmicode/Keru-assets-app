import { View, Text } from 'react-native'
import React from 'react'

const AddDriverSheet = ({ driverCloseSheet }) => {
  return (
    <View>
      <Text>AddDriverSheet</Text>
    </View>
  );
};

export default AddDriverSheet